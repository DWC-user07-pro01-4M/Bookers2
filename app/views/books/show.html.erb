<!--books/show-->
<h1>Book detail</h1>
<div>
  <p><%= Book.find(params[:id]).title %></p>
  <p><%= Book.find(params[:id]).body %></p>


      <%= link_to "Edit", edit_book_path %>
      <%= link_to "Destroy",book_path,method: :delete %>

</div>

<%= form_with model: @book do |f| %>

    <h4>Title</h4><br>
      <%= f.text_field :title %>

    <h4>Opinion</h4><br>
      <%= f.text_area :body %>

    <%= f.submit 'Create Book' %>

  <% end %>